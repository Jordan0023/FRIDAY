# Firmware Audit: GL.iNet GL-MT5000 Brume 3 / mt5000-4.8.6_release2-883-0422-1776826783.bin

- Source URL: https://fw.gl-inet.com/firmware/mt5000/release/mt5000-4.8.6_release2-883-0422-1776826783.bin
- Local path: known_firmware/firmware/GL.iNet_GL-MT5000_Brume_3/mt5000-4.8.6_release2-883-0422-1776826783.bin
- SHA-256: `001503668d83a939627f6bd92614fff417251f9f517e011d14abe2fb0a307417`
- Size: 64082622 bytes
- Version: 4.8.6
- Release date: 2026-04-24 17:38:45

## Static Findings

### Hard-coded credentials or secrets

Hard-coded secrets should be reviewed for default credentials, service tokens, or recoverable admin passwords.

Evidence: `password`

### HTTP CGI/admin attack surface

Web admin components should be reviewed for authentication bypass, command injection, and unsafe parameter parsing.

Evidence: `bOA`

### Command execution helpers

Command execution paths are common command-injection sinks when reachable from web or network inputs.

Evidence: `System, eval, popen, system`

### Potential memory-unsafe C functions

Unsafe C library calls can indicate buffer overflow risk; confirm whether inputs are bounded before use.

Evidence: `sprintf, strcat, strcpy`

## Zero-day Triage

This section separates known-vulnerability coverage from new candidate hunting. Statuses here are leads until a route-specific data flow or live PoC confirms exploitability.

### Known Vulnerability Coverage

No known-vulnerability catalog entries matched this product name.
### High-priority New Candidates

No route candidates were identified.
### Sink Summary

- exec: 329
- file_write: 303
- popen: 72
- system: 985
- unsafe_copy: 126

### Config Writer Leads

- `82_merge_mtk_sdk: local enable=$(uci -q get firewall.@defaults[0].flow_offloading_hw)`
- `82_merge_mtk_sdk: uci commit firewall`
- `82_merge_mtk_sdk: uci delete firewall.@defaults[0].flow_offloading`
- `82_merge_mtk_sdk: uci delete firewall.@defaults[0].flow_offloading_hw`
- `82_merge_mtk_sdk: uci set firewall.@defaults[0].flow_offloading='1'`
- `82_merge_mtk_sdk: uci set firewall.@defaults[0].flow_offloading_hw='1'`
- `K05samba4: /etc/samba/smb.conf.template > /var/etc/smb.conf`
- `K05samba4: [ -e /etc/samba/smb.conf ] || ln -nsf /var/etc/smb.conf /etc/samba/smb.conf`
- `K05samba4: if [ ! -L /etc/samba/smb.conf ]; then`
- `K05samba4: if [ ! -e /etc/samba/smb.conf ]; then`
- `K05samba4: logger -p daemon.error -t 'samba4-server' "missing config /etc/samba/smb.conf!"`
- `K05samba4: logger -p daemon.warn -t 'samba4-server' "Local custom /etc/samba/smb.conf file detected, all luci/config settings are ignored!"`
- `K05samba4: procd_set_param file /etc/samba/smb.conf`
- `K05samba4: socket_opt="$(grep -i 'socket options' /etc/samba/smb.conf.template | awk -F'=' '{print $2}' | tr -d '\n')"`
- `K05samba4: } >> /var/etc/smb.conf`
- `K10openvpn: $(openvpn_get_credentials "$name" "$conf")`
- `K10openvpn: $(openvpn_get_dev "$name" "$conf") \`
- `K10openvpn: ${client:+--ipchange "/usr/libexec/openvpn-hotplug ipchange $name"} \`
- `K10openvpn: --down "/usr/libexec/openvpn-hotplug down $name" \`
- `K10openvpn: --route-pre-down "/usr/libexec/openvpn-hotplug route-pre-down $name" \`
- `K10openvpn: --route-up "/usr/libexec/openvpn-hotplug route-up $name" \`
- `K10openvpn: --status "/var/run/openvpn.$name.status" \`
- `K10openvpn: --syslog "openvpn($name)" \`
- `K10openvpn: --up "/usr/libexec/openvpn-hotplug up $name" \`
- `K10openvpn: . /lib/functions/openvpn.sh`
- `K10openvpn: . /usr/share/openvpn/openvpn.options`
- `K10openvpn: PATH_INSTANCE_DIR="/etc/openvpn"`
- `K10openvpn: PROG=/usr/sbin/openvpn`
- `K10openvpn: [ "$v" = "frames_only" ] && [ "$p" = "compress" ] && unset v && append_param "$s" "$p" && echo >> "/var/etc/openvpn-$s.conf"`
- `K10openvpn: [ "$v" = 1 ] && append_param "$s" "$p" && echo >> "/var/etc/openvpn-$s.conf"`

### Triage Notes

- Evidence levels: L0 route/string present, L1 parameters identified, L2 dangerous sink present in firmware, L3 route/parameter-to-sink flow should be proven with decompiler, L4 filter bypass plausible, L5 live PoC confirmed.
- This triage increases zero-day coverage but does not replace route-specific reverse engineering or live emulation.

Structured zero-day triage JSON: `known_firmware/reports/GL.iNet_GL-MT5000_Brume_3/mt5000-4.8.6_release2-883-0422-1776826783.bin.zero-day.json`

## Extraction Notes

- binwalk -eM --directory known_firmware/extracted/001503668d83a939 stopped: extraction exceeded 768 MB

## Decompiler Notes

- /home/jordan/Tools/ghidra_12.1.2_PUBLIC/support/analyzeHeadless known_firmware/ghidra_projects GL.iNet_GL-MT5000_Brume_3_00150366 -import exited 0: OpenJDK 64-Bit Server VM (build 21.0.11+10-1-22.04.2-Ubuntu, mixed mode)

## Validation Needed

These are static-analysis leads. Confirm reachability, affected versions, exploit preconditions, and vendor-fixed versions before treating any item as a vulnerability.
